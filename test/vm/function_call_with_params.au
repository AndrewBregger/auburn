let one = 1.0
let two = 2.0
let three = 3.0
let four = 4.0

fn test(one f32, two f32, three f32, four f32) {
	print one + three
	print four / two
	print four / two - two
}

fn main() {
	test(one, two, three, four)
}
